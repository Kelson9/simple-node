language: node_js
node_js:
 - 13

 services:
  - docker

#Pre-testing insatalla
install:
  - echo "nothing needs to be installed"

#scripts to be run such as tests
before_script:
  - echo  "no tests"

script:
  - docker --version # print the version for logging
  - docker build -t simple-node .
  - docker tag simple-node kelson9/simple-node:latest

after_success:
  - echo "$DOCKER_PASSWORD"  | docker login -u  "$DOCKER_USERNAME"  --password-stdin
  - docker push kelson9/simple-node

#deploy
".travis.yml" 29L, 579C    